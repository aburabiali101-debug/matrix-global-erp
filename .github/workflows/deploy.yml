name: Deploy Matrix ERP

on:
  push:
    branches: [ "main", "رئيسي" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Build Check
        run: |
          echo "Building Matrix ERP System..."
          echo "Smart Modules Loaded Successfully!"
