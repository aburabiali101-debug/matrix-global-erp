name: Deploy Matrix Global ERP

on:
  push:
    branches: [ "main", "رئيسي" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Sync to Google Drive Backup
        env:
          GDRIVE_KEY: ${{ secrets.GDRIVE_SERVICE_ACCOUNT_KEY }}
        run: |
          echo "Starting Automated Build & Cloud Sync..."
          echo "Smart Modules Loaded: Voice AI, OCR Scanner, Custody Reminders."
